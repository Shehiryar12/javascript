const idcard=[
    [32,21],
    ['usman','ahmed'],
    [18,25],
]
console.log(Object.fromEntries(idcard))

// it is used to convert array into object, when we print them in object the numbers in array will print.